<h1>New Artist</h1>
<hr/>

<%= render 'form', artist: @artist %>

<div class="row">
  <div class="col-sm-1">
    <%= button_to("Back", artists_path, method: :get, :class => "btn btn-success") %>
  </div>
</div>