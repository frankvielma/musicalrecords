<%= form_for(lp) do |f| %>
  <% if lp.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lp.errors.count, "error") %> prohibited this lp from being saved:</h2>

      <ul>
      <% lp.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="field">
    <%= f.select :artist_id, options_for_select(@artists.all.map{|s|[s.name, s.id]}, selected: f.object.artist_id) %>

  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
